server:
  port: 8080

cors:
  allowed-origins: http://localhost:4200

ytdlp:
  path: yt-dlp
  temp-dir: ${java.io.tmpdir}/videograb
  timeout: 120
  max-filesize: 500M

rate-limit:
  requests-per-minute: 15

spring:
  cache:
    type: caffeine
